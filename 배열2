1. `배열의 합 구하기`
**const add = arr => arr.reduce((a, b) => a + b, 0);**
add라는 변수에 합 구하는 함수 저장 add(arr)로 합 반환

2. `배열에서 숫자 골라내기`
const nums = my_string.match(/[0-9]+/g);

3. 배열이 [ ] 이렇게 비어있으면 길이가 0 인거고 아예 존재하지 않으면 !으로 확인

4.`두 배열의 요소가 같은지` 보려면 정렬해서 두 배열이 같은지 확인
이때 배열은 참조타입이라 그냥 같다하면 다름 → JSON.stringify.
객체의 경우 배열로 바꿔서 정렬한 후 이걸로 같은지 비교

5.  이차원 배열 : const arr2 = Array.from({ length: 3 }, () => [ ]);

6.
